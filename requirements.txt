# CrowdSim 의존성
# 설치: python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
# 참고: nextlib는 외부 경로가 아닌 lib/nextlib(프로젝트 동봉)에서 임포트됨 (main.py 참조)
PySide6>=6.7
vtk>=9.3
numpy
opencv-python

# lib/nextlib(동봉된 nextlib 서브셋)가 필요로 하는 의존성
psutil
matplotlib
